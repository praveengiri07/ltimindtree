<div class="collection">
    <h2 class="collection-title">{{ collections['frontpage'].title }}</h2>

    <div class="row collection-row">
        {% for product in collections['frontpage'].products %}
            {% render 'product-item', product: product %}  
        {% endfor %}
    </div>
</div>
